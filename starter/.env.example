# AuroraApp — variables para docker compose (starter/)
# La BD real se define SOLO en backend/.env → DATABASE_URL (sin Postgres local).

JWT_SECRET=change-me-to-a-long-random-production-secret

VITE_API_URL=/api/v1

# Opcional — asistente IA (también puede vivir en backend/.env)
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-haiku-4-5-20251001
